services:
  - type: web
    name: compplan
    runtime: node
    nodeVersion: "20"
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
